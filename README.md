# 2025 흔적 주식게임 📈 - Real-time Synchronization Edition

React + Socket.io 기반의 실시간 동기화 투자 시뮬레이터입니다. 60명의 플레이어가 동시에 연결하여 관리자가 제어하는 게임 상태를 실시간으로 동기화합니다.

## 🚀 시작하기

### 설치

```bash
npm install
```

### 개발 서버 실행 (서버 + 클라이언트 동시 실행)

```bash
npm run dev:all
```

또는 개별 실행:

```bash
# 터미널 1: 서버 실행
npm run server

# 터미널 2: 클라이언트 실행
npm run dev
```

### 접속 URL

- **플레이어 페이지 (메인)**: `http://localhost:5173/` 또는 `http://localhost:5173/player`
- **관리자 페이지**: `http://localhost:5173/admin`

### 빌드

```bash
npm run build
npm run server
```

## 📋 주요 기능

### 1. 실시간 동기화 (Real-time Synchronization)
- **Socket.io 기반**: 서버에서 게임 상태를 관리하고 모든 클라이언트에 실시간 브로드캐스트
- **동기화된 게임 진행**: 관리자가 라운드를 진행하면 모든 플레이어 화면에 동시에 반영
- **플레이어 수 모니터링**: 관리자 화면에서 현재 접속한 플레이어 수 확인

### 2. 관리자 대시보드 (Admin Dashboard)
- **큰 화면 최적화**: 4K 빅 스크린을 위한 고가시성 디스플레이
- **게임 제어**: 게임 시작 및 라운드 진행 버튼
- **실시간 차트**: 5개 주식의 가격 추이를 보여주는 멀티라인 차트
- **뉴스 모달**: 라운드 변경 시 모든 플레이어에게 동시에 표시되는 헤드라인 모달
- **뉴스 티커**: 하단에 스크롤되는 뉴스 텍스트
- **시나리오 에디터**: 게임 중 실시간으로 변동률과 헤드라인 수정 가능
- **플레이어 리스트**: 모든 플레이어의 현금, 보너스 포인트, 총 자산, 거래 횟수 표시
- **포인트 추가**: 각 플레이어에게 보너스 포인트 추가 가능

### 3. 플레이어 클라이언트 (Player Client)
- **닉네임 로그인**: 간단한 닉네임 입력으로 게임 참여
- **실시간 동기화**: 관리자가 진행하는 라운드와 가격 정보를 실시간으로 수신
- **주식 매수/매도**: 각 주식마다 매수/매도 기능
- **자동 계산**: 총 자산 = 현금 + 보너스 포인트 + Σ(수량 × 현재가)
- **수익률 표시**: 이전 라운드 대비 손익률 표시
- **포트폴리오 요약**: 주식별 현재가, 보유 수량, 평가 금액 테이블
- **서버 기반 관리**: 모든 거래가 서버에서 기록되고 검증됨

### 4. 게임 엔진
- **12개월 시나리오**: 2025년 1월~12월 MZ 트렌드를 반영한 이벤트
  - 1월: GPT-5 출시
  - 2월: 직구 금지법
  - 3월: 아이돌 스캔들
  - 4월: 비트코인 폭락
  - 5월: 영생 바이오 약물
  - 6월: 오징어 게임 2
  - 7월: 구독료 인상
  - 8월: 암호화폐 재상장
  - 9월: 명품 가격 하락
  - 10월: 탈모 완치제
  - 11월: 테슬라 봇
  - 12월: 산타 랠리 vs 위기
- **5개 섹터**: AI테크, K-도파민, 코인/밈, 영생바이오, 리셀/럭셔리
- **서버 기반 가격 계산**: 모든 클라이언트가 동일한 가격 데이터를 받도록 서버에서 계산
- **누적 가격 계산**: 각 라운드 가격 = 이전 라운드 가격 × (1 + 변동률)

## 🎨 디자인

- **테마**: 모던 글래스모피즘 / 사이버펑크 스타일
- **배경**: 다크 그라데이션 (슬레이트-퍼플)
- **상승**: 빨간색 (#ef4444) - 네온 효과
- **하락**: 파란색 (#3b82f6) - 네온 효과
- **강조**: 그라데이션 텍스트 (퍼플-핑크) - 헤드라인 및 중요 정보
- **카드**: 글래스모피즘 (반투명 + 블러 효과)

## 📁 프로젝트 구조

```
game-cursor/
├── server.js                    # Socket.io 서버 (Express)
├── src/
│   ├── pages/
│   │   ├── AdminPage.jsx        # 관리자 페이지
│   │   └── PlayerPage.jsx        # 플레이어 페이지 (메인)
│   ├── components/
│   │   ├── StockCard.jsx        # 주식 카드 컴포넌트
│   │   ├── NewsModal.jsx        # 뉴스 모달
│   │   ├── NewsTicker.jsx       # 뉴스 티커
│   │   └── ScenarioEditor.jsx   # 시나리오 에디터
│   ├── hooks/
│   │   └── useSocketSync.js     # Socket.io 동기화 훅
│   ├── data/
│   │   └── initialScenarios.js  # 초기 시나리오 데이터
│   ├── App.jsx                 # 메인 앱 컴포넌트 (라우팅)
│   ├── main.jsx                 # 진입점
│   └── index.css                # 글로벌 스타일
└── package.json
```

## 🎮 사용 방법

### 서버 실행
1. `npm run dev:all` 명령으로 서버와 클라이언트를 동시에 실행
2. 또는 별도 터미널에서 `npm run server`와 `npm run dev` 실행

### 관리자 모드
1. 브라우저에서 `http://localhost:5173/admin` 접속
2. 자동으로 관리자로 인증됨
3. "게임 시작" 버튼으로 게임 시작
4. "다음" 버튼으로 라운드 진행 (모든 플레이어에게 동시에 반영)
5. 우측 상단의 설정 아이콘으로 시나리오 편집 가능
6. 좌측 상단에서 접속한 플레이어 수 확인
7. 플레이어 리스트에서 각 플레이어에게 포인트 추가 가능

### 플레이어 모드
1. 브라우저에서 `http://localhost:5173/` 또는 `http://localhost:5173/player` 접속
2. 닉네임을 입력하고 "입장하기" 클릭
3. 각 주식의 매수/매도 수량을 입력하고 버튼 클릭
4. 관리자가 라운드를 진행하면 자동으로 가격이 업데이트됨
5. 실시간으로 총 자산과 수익률이 계산됨

## 🔧 기술 스택

### Frontend
- **React 18**: UI 라이브러리
- **Vite**: 빌드 도구
- **Tailwind CSS**: 스타일링
- **Framer Motion**: 애니메이션
- **Recharts**: 차트 라이브러리
- **Lucide React**: 아이콘
- **Socket.io Client**: 실시간 통신

### Backend
- **Node.js**: 서버 런타임
- **Express**: 웹 서버 프레임워크
- **Socket.io**: 실시간 양방향 통신
- **CORS**: Cross-Origin 리소스 공유

## 📝 시나리오 커스터마이징

`src/data/initialScenarios.js` 파일에서 시나리오를 수정할 수 있습니다:

- `month`: 각 라운드의 월 (1월~12월)
- `headline`: 각 라운드의 뉴스 헤드라인
- `volatility`: 각 섹터별 변동률 (%)

또는 관리자 페이지에서 실시간으로 시나리오를 편집할 수 있습니다.

## 🔌 Socket.io 이벤트

### 서버 → 클라이언트
- `GAME_STATE_UPDATE`: 게임 상태 업데이트
- `ADMIN_AUTH_SUCCESS`: 관리자 인증 성공
- `PLAYER_COUNT_UPDATE`: 플레이어 수 업데이트 (관리자 전용)
- `PLAYER_LIST_UPDATE`: 플레이어 리스트 업데이트 (관리자 전용)
- `PLAYER_PORTFOLIO_UPDATE`: 플레이어 포트폴리오 업데이트
- `TRANSACTION_ERROR`: 거래 오류
- `GAME_END`: 게임 종료

### 클라이언트 → 서버
- `ADMIN_AUTH`: 관리자 인증 요청
- `PLAYER_JOIN`: 플레이어 접속 (닉네임 포함)
- `ADMIN_START_GAME`: 게임 시작
- `ADMIN_NEXT_ROUND`: 다음 라운드
- `ADMIN_PREVIOUS_ROUND`: 이전 라운드
- `ADMIN_UPDATE_SCENARIO`: 시나리오 업데이트
- `ADMIN_ADD_POINTS`: 플레이어에게 포인트 추가
- `ADMIN_REQUEST_PLAYER_LIST`: 플레이어 리스트 요청
- `PLAYER_BUY_STOCK`: 주식 매수
- `PLAYER_SELL_STOCK`: 주식 매도

## 🎯 향후 개선 사항

- [ ] 랭킹 시스템 (플레이어별 총 자산 순위)
- [ ] 데이터 내보내기/가져오기
- [ ] 음향 효과
- [ ] 다중 게임 세션 지원
- [ ] 관리자 비밀번호 인증

## 📄 라이선스

MIT


